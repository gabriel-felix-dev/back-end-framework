# Crie 3 páginas HTML com conteúdo chamadas:

- login.html(página de login, simule o login com usuário admin e com usuário comum)

- cadastro.html(página de cadastro)

- home.html (Home page exibe o nome vindo do login)
    - No home.html deve existir um link para o usuário fazer o log out.

Crie as rotas para renderização de cada página e os links de navegação entre elas.